{
  "if": {
    "properties": { "$LANG": { "const": "c" } }
  },
  "then": {
      "files": [
    {
      "pattern": "cprograms/*.out",
      "target": "example-repo-local/${JOB_NAME}/${BUILD_NUMBER}/",
      "flat" : "false"
    }
  ]
  },
}
