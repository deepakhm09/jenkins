{
  "files": [
    {
      "pattern": "*.out",
      "target": "example-repo-local/${JOB_NAME}/${BUILD_NUMBER}/",
      "flat" : "false"
    }
  ]
  
  "files": [
    {
      "pattern": "*.wic",
      "target": "example-repo-local/${JOB_NAME}/${BUILD_NUMBER}/",
      "flat" : "false"
    }
  ]
}
